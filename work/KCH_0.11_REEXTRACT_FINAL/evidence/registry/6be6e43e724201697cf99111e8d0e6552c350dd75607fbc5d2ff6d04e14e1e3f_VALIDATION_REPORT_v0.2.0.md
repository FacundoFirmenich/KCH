# Validation report v0.2.0

## Puertas ejecutadas

1. Compilacion de todos los modulos bajo `src`.
2. Regresion del runtime volumetrico heredado.
3. Casos adversariales de los operadores nuevos.
4. Conformidad MCP real por `stdio`.
5. Construccion de wheel sin aislamiento de build ambiental.
6. Instalacion e importacion desde destino aislado.

## Bateria

La bateria final concluyo:

```text
98 passed in 2.76s
```

Los 51 controles nuevos cubren:

- clasificacion de actos semanticos;
- resistencia a recencia;
- roles `CORE`, `FRAME` y `TACIT`;
- invariantes y supersesion;
- alcance condicional;
- Top-2 y aclaracion;
- contradiccion y SURF;
- prohibiciones de interaccion;
- compactacion valida y compactaciones adversariales;
- estados de soporte y sobreinferencia;
- politicas de supuestos y conflictos locales;
- activacion, cadena y readback del refresco;
- gobernador compuesto;
- persistencia de estado durante llamadas MCP.

## Conformidad MCP

El servidor se ejecuto como proceso `stdio`, no solo como llamada en memoria.

```text
protocol = 2024-11-05
tools = 39
clean_output_preflight = APPLY
```

## Distribucion

```text
wheel = kwancode_csi_operator_suite-0.2.0-py3-none-any.whl
sha256 = 6bf3d88e157b906e562fed4cd275c386dab35ce5f785bb9442982c20d59e5b0e
isolated_import = PASS
```

El primer intento de build quedo esperando dependencias ambientales. No produjo evidencia. La repeticion con `--no-build-isolation` uso el toolchain ya instalado y completo correctamente.

## Incidencias corregidas

- Una insercion incremental dejo una declaracion MCP sintacticamente invalida; `compileall` la bloqueo antes de pruebas.
- La primera bateria nueva uso un directorio temporal externo no permitido y una clave de asercion incorrecta para `TOP_2`. Se traslado el temporal al workspace y se alineo la asercion con el contrato publico `alternatives`.
- La bateria final limpia no conserva fallos ni errores.

## Adjudicacion

La suite puntual queda validada para iniciar macroconvergencia. Esta adjudicacion no promueve las ramas externas OP-01 y OP-03 ni autoriza claims fuera de los limites documentados.

