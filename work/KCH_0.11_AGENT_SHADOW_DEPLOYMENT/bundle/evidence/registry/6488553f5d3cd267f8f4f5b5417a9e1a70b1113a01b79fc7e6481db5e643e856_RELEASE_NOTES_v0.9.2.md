# KHC v0.9.2

Adds an opt-in fail-closed structural stage to `kwancode_output_preflight`: non-empty output, JSON parseability, required top-level keys, result count, and exact task coverage/order. Existing callers remain backward compatible.
